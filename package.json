{
  "name": "@stackla/public-types",
  "version": "1.0.8",
  "description": "Public types for UGC Widgets",
  "types": "dist/src/widgets/index.d.ts",
  "repository": "https://github.com/Stackla/public-types",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/@stackla"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --declaration --declarationMap --emitDeclarationOnly --outDir dist",
    "prepare": "rm -r dist && npm run build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "files": [
    "dist"
  ]
}
